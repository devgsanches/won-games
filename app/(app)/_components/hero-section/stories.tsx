import { Meta, StoryObj } from '@storybook/react'

import { HeroSection } from '.'

export default {
  title: 'app/(app)/_components/hero-section',
  component: HeroSection,
  tags: ['autodocs'],
  parameters: {
    layout: 'fullscreen'
  }
} as Meta

export const Default: StoryObj = {}
