import { Meta, StoryObj } from '@storybook/react'
import { TypingText } from '.'

export default {
  title: 'app/(app)/_components/TypingText',
  component: TypingText,
  tags: ['autodocs'],
  parameters: {
    layout: 'fullscreen'
  }
} as Meta
export const Default: StoryObj = {}
