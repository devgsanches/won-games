import { Meta, StoryObj } from '@storybook/react'
import { Showcase } from '.'

export default {
  title: 'app/(app)/_components/Showcase',
  component: Showcase,
  tags: ['autodocs'],
  parameters: {
    layout: 'fullscreen'
  }
} as Meta
export const Default: StoryObj = {}
