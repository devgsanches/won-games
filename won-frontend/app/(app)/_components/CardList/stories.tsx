import { Meta, StoryObj } from '@storybook/react'
import { CardList } from '.'

export default {
  title: 'app/(app)/_components/CardList',
  component: CardList,
  tags: ['autodocs'],
  parameters: {
    layout: 'fullscreen'
  }
} as Meta
export const Default: StoryObj = {}
