import { Meta, StoryObj } from '@storybook/react'
import { CartIcon } from '.'

export default {
  title: 'app/(app)/_components/CartIcon',
  component: CartIcon,
  tags: ['autodocs'],
  parameters: {
    layout: 'fullscreen'
  }
} as Meta
export const Default: StoryObj = {}
