import { Meta, StoryObj } from '@storybook/react'
import { ProfileOrders } from '.'

export default {
  title: 'app/(app)/_components/ProfileOrders',
  component: ProfileOrders,
  tags: ['autodocs'],
  parameters: {
    layout: 'fullscreen'
  }
} as Meta
export const Default: StoryObj = {}
