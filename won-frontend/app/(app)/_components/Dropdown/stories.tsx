import { Meta, StoryObj } from '@storybook/react'
import { Dropdown } from '.'

export default {
  title: 'app/(app)/_components/Dropdown',
  component: Dropdown,
  tags: ['autodocs'],
  parameters: {
    layout: 'fullscreen'
  }
} as Meta
export const Default: StoryObj = {}
