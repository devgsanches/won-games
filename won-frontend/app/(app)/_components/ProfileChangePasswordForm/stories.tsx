import { Meta, StoryObj } from '@storybook/react'
import { ProfileChangePasswordForm } from '.'

export default {
  title: 'app/(app)/_components/ProfileChangePasswordForm',
  component: ProfileChangePasswordForm,
  tags: ['autodocs'],
  parameters: {
    layout: 'fullscreen'
  }
} as Meta
export const Default: StoryObj = {}
