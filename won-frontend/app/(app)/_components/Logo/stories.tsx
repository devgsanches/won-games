import { Meta, StoryObj } from '@storybook/react'
import { Logo } from '.'

export default {
  title: 'app/(app)/_components/Logo',
  component: Logo,
  tags: ['autodocs'],
  parameters: {
    layout: 'fullscreen'
  }
} as Meta
export const Default: StoryObj = {}