import { Meta, StoryObj } from '@storybook/react'
import { WritingText } from '.'

export default {
  title: 'app/(app)/_components/WritingText',
  component: WritingText,
  tags: ['autodocs'],
  parameters: {
    layout: 'fullscreen'
  }
} as Meta
export const Default: StoryObj = {}
