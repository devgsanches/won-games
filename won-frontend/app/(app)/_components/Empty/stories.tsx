import { Meta, StoryObj } from '@storybook/react'
import { Empty } from '.'

export default {
  title: 'app/(app)/_components/Empty',
  component: Empty,
  tags: ['autodocs'],
  parameters: {
    layout: 'fullscreen'
  }
} as Meta
export const Default: StoryObj = {}
