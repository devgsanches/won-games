import { Meta, StoryObj } from '@storybook/react'
import { ShowMore } from '.'

export default {
  title: 'app/(app)/_components/ShowMore',
  component: ShowMore,
  tags: ['autodocs'],
  parameters: {
    layout: 'fullscreen'
  }
} as Meta
export const Default: StoryObj = {}
