import { Meta, StoryObj } from '@storybook/react'
import { ProfileForm } from '.'

export default {
  title: 'app/(app)/_components/ProfileForm',
  component: ProfileForm,
  tags: ['autodocs'],
  parameters: {
    layout: 'fullscreen'
  }
} as Meta
export const Default: StoryObj = {}
